<ul>
  <% @doses.each do |dose| %>
  <li><%= dose.ingredient.name %> - <%= dose.description %></li>
  <% end %>
</ul>

<%= link_to 'Add dose', new_cocktail_dose_path(@cocktail) %><%= link_to 'Go back', cocktail_path(@cocktail) %>
