<%= simple_form_for [@cocktail, @dose], url: { controller: 'doses', action: 'create', format: :js }, remote: true do |f| %>
  <%= f.association :ingredient, collection: Ingredient.all %>
  <%= f.input :description %>
  <%= f.button :submit %>
<% end %>
